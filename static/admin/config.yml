backend:
  name: git-gateway
  branch: netlifycms


media_folder: "static/images" # Folder where user uploaded files should go
public_folder: "/images"


collections: # A list of collections the CMS should be able to edit
###################### Posts Collection ######################
  - name: "blog" # Used in routes, ie.: /admin/collections/:slug/edit
    label: "Blog" # Used in the UI, ie.: "New Post"
    label_singular: "Post" # Used in the UI, ie: "New Post"
    description: >
      Добавление и редактирование в разделе Блог.
    folder: "content/ru/blog" # The path to the folder where the documents are stored
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}" # Name of post file in folder
    path: "{{slug}}/index"
    create: true # Allow users to create new documents in this collection
    #filter: {field: "cmsedit", value: true}
    media_folder: ""
    public_folder: ""
    fields: # The fields each document in this collection have
      - { label: "1File name", name: "slug", widget: "string"  }
      - { label: "2CMSedit", name: "cmsedit", widget: "hidden", default: true  }
      - { label: "3Title", name: "title", widget: "string"  }
      - { label: "4Lead", name: "lead", widget: "string"  }
      - { label: "5Publish Date", name: "date", widget: "datetime"  }
      - { label: "6Last Modified", name: "lastmod", widget: "datetime"  }
      - { label: "7Draft", name: "draft", widget: "boolean", default: false  }
      - { label: "8Body", name: "body", widget: "markdown"  }


